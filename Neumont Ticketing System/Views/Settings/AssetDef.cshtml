@model Neumont_Ticketing_System.Views.Settings.AssetDefModel
@{
    ViewData["Title"] = "AssetDef";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<link href="~/css/AssetDef.css" rel="stylesheet" type="text/css" />

<h1>Asset Definitions</h1>

<div class="interactiveList" id="typesList">
    <h2 class="interactiveListTitle">Asset Types</h2>
    @foreach (var type in Model.AssetTypes)
    {
        <div class="interactiveListItem">
            <div class="inputContainer">
                <p class="inputLabel">Name: </p>
                @Html.EditorFor(model => type.Name)
            </div>
            <div class="inputContainer">
                <p class="inputLabel">Description: </p>
                @Html.EditorFor(model => type.Description)
            </div>
        </div>
    }
</div>

<div class="interactiveList" id="typesList">
    <h2 class="interactiveListTitle">Asset Manufacturers</h2>
    @for (int i = 0; i < Model.AssetManufacturers.Count; i++)
    {
        var mfr = Model.AssetManufacturers[i];
        <div class="interactiveListItem">
            <div class="inputContainer">
                <p class="inputLabel">Name: </p>
                @Html.EditorFor(model => mfr.Name)
            </div>
            <div class="inputContainer">
                <p class="inputLabel">Phone Numbers: </p>
                @for (int j = 0; j <= mfr.PhoneNumbers.Count; j++)
                {
                    if (j >= mfr.PhoneNumbers.Count)
                    {
                        <input class="text-box single-line" id=@($"mfr{i}_PhoneNumbers_{j}")
                               name=@($"mfr{i}.PhoneNumbers[{j}]") type="tel" value="" />
                    }
                    else
                    {
                        <input class="text-box single-line" id=@($"mfr{i}_PhoneNumbers_{j}")
                               name=@($"mfr{i}.PhoneNumbers[{j}]") type="tel" value=@(mfr.PhoneNumbers[j]) />
                    }
                }
            </div>
            <div class="inputContainer">
                <p class="inputLabel">Email addresses: </p>
                @for (int j = 0; j <= mfr.EmailAddresses.Count; j++)
                {
                    if (j >= mfr.EmailAddresses.Count)
                    {
                        <input class="text-box single-line" id=@($"mfr{i}_EmailAddresses_{j}")
                               name=@($"mfr{i}.EmailAddresses[{j}]") type="email" value="" />
                    }
                    else
                    {
                        <input class="text-box single-line" id=@($"mfr{i}_EmailAddresses_{j}")
                               name=@($"mfr{i}.EmailAddresses[{j}]") type="email" value=@(mfr.EmailAddresses[j]) />
                    }
                }
            </div>
        </div>
    }
</div>

<script src="~/js/AssetDef.js"></script>